[project]
name = "prove-api"
version = "1.0.0"
description = "ProVe User Service — Flask API, dashboard, and queue management"
requires-python = ">=3.10"

dependencies = [
    "prove-shared",
    "flask",
    "flask-cors",
    "flasgger",
    "flask-sqlalchemy",
    "dash",
    "dash-bootstrap-components",
    "plotly",
    "schedule",
]
